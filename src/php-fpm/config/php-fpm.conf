[global]
; Do not daemonize – container process must stay in foreground.
daemonize = no

; Forward errors to the container log collector via stderr.
error_log = /proc/self/fd/2
log_level = notice

; Include the worker pool definitions.
include = /etc/php-fpm-pool.d/*.conf
